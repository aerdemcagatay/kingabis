Enter these commands in the home directory of the server (same place this file is)

node .
    This runs the server instance

npm test
    This runs mocha tests through all .js files the tests in the /test folder
